生成代码提示词
帮忙给的表生成代码，服务为otc，模块为t043/receipt，注意下各个工程的包路径可能不一样，需要先自己找下包路径



数据库表单脚本如下：

CREATE TABLE YLADMIN.T_RMBS_RECEIPT_SUB_VENDOR_INFO (
ID NUMBER,
AGENT_VENDOR_NAME VARCHAR2(240),
AGENT_VENDOR_CODE VARCHAR2(30),
AGENT_VENDOR_SITE_ID NUMBER(38,0),
AGENT_VENDOR_SITE_CODE VARCHAR2(60),
AGENT_VENDOR_SITE VARCHAR2(200),
AMOUNT NUMBER(20,2) DEFAULT 0,
TRANSACTION_DATE TIMESTAMP,
BANK_SERIAL_NUMBER VARCHAR2(50),
REMARK VARCHAR2(100),
RECEIPT_NUM VARCHAR2(50),
RECEIPT_ID NUMBER,
CLAIM_ID NUMBER,
ACCEPTANCE_DATE TIMESTAMP,
DUE_DATE TIMESTAMP,
TENANT_CODE VARCHAR2(50),
CONSTRAINT RECEIPT_SUB_VENDOR PRIMARY KEY (ID)
);

CREATE TABLE YLADMIN.T_RMBS_RECEIPT_SUB_INFO (
	ID NUMBER,
	RECEIPT_ACCOUNT_NUM VARCHAR2(25),
	RECEIPT_BANK_NAME VARCHAR2(100),
	RECEIPT_SUB_ACCOUNT_NUM VARCHAR2(20),
	RECEIPT_SUB_ACCOUNT_NAME VARCHAR2(100),
	NAME_METHOD VARCHAR2(50),
	APPLY_AMOUNT NUMBER(20,2) DEFAULT 0,
	AUTO_APPLY VARCHAR2(1),
	CLAIM_ID NUMBER,
	IS_IMPORT VARCHAR2(2),
	RECEIPT_METHOD_ID VARCHAR2(20),
	RECEIPT_BANK_ACCT_USE_ID NUMBER,
	PAYMENT_BANK_ACCOUNT_ID NUMBER,
	CE_ITEM VARCHAR2(50),
	TENANT_CODE VARCHAR2(50),
	CONSTRAINT RECEIPT_SUB PRIMARY KEY (ID)
);

页面字段提供截图
没有就跳过

约束：
1，不生成controller,service
2,生成到claim-otc模块下

生成完需要确认,注意有两张表