生成代码提示词
帮忙给的表生成代码，服务为otc，模块为claim/t047/quy，注意下各个工程的包路径可能不一样，需要先自己找下包路径



数据库表单脚本如下：
CREATE TABLE "YLADMIN"."T_QUY_RA_CUST_TRX_TYPE"
(	"ID" VARCHAR2(100) NOT NULL ENABLE,
"CUSTTRXTYPEID" VARCHAR2(100),
"ORGID" VARCHAR2(100),
"SETOFBOOKSID" VARCHAR2(100),
"LEGALENTITYID" VARCHAR2(100),
"NAME" VARCHAR2(100),
"DESCRIPTION" VARCHAR2(100),
"STARTDATE" DATE,
"ENDDATE" DATE,
"TYPE" VARCHAR2(100),
"CREATIONSIGN" VARCHAR2(100),
"DEFAULTSTATUS" VARCHAR2(100),
"PRINTINGOPTION" VARCHAR2(100),
"CREDITMEMOTYPEID" VARCHAR2(100),
"TERMID" VARCHAR2(100),
"ACCOUNTINGAFFECTFLAG" VARCHAR2(100),
"ADJPOSTTOGL" VARCHAR2(100),
"POSTTOGL" VARCHAR2(100),
"ALLOWFREIGHTFLAG" VARCHAR2(100),
"NATURALAPPLICATIONONLYFLAG" VARCHAR2(100),
"TAXCALCULATIONFLAG" VARCHAR2(100),
"EXCLUDEFROMLATECHARGES" VARCHAR2(100),
"ALLOWOVERAPPLICATIONFLAG" VARCHAR2(100),
"CASHFLOWITEM" VARCHAR2(100),
"INVOICEPRINTREQUIRED" VARCHAR2(100),
"AUTOAPPLIED" VARCHAR2(100),
"REVERSECATEGORY" VARCHAR2(100),
"GLIDCLEARING" VARCHAR2(100),
"GLIDFREIGHT" VARCHAR2(100),
"GLIDREC" VARCHAR2(100),
"GLIDREV" VARCHAR2(100),
"GLIDTAX" VARCHAR2(100),
"GLIDUNBILLED" VARCHAR2(100),
"GLIDUNEARNED" VARCHAR2(100),
"LASTUPDATEDDATE" DATE,
"TENANT_CODE" VARCHAR2(50),
PRIMARY KEY ("ID")
USING INDEX PCTFREE 10 INITRANS 2 MAXTRANS 255 COMPUTE STATISTICS
STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1
BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
TABLESPACE "YLTABLESPACE"  ENABLE
)

页面字段提供截图
没有就跳过

约束：
1，不生成controller
2,生成到claim-otc模块下

生成完需要确认,注意有两张表